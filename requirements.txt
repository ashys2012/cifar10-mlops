# CIFAR-10 ResNet18 — Python 3.12
# Exact versions from the working development environment.
#
# Install:
#   uv venv --python 3.12
#   source .venv/bin/activate
#   uv pip install -r requirements.txt
#
# Then install ZenML integrations (mlflow + pytorch materializer):
#   zenml integration install mlflow pytorch -y
#
# Note: torch and torchvision are pinned to the CUDA 12.1 builds.
# For CPU-only machines, replace with:
#   torch==2.5.1
#   torchvision==0.20.1

# ---------------------------------------------------------------------------
# API server
# ---------------------------------------------------------------------------
fastapi==0.120.4
uvicorn==0.49.0
python-multipart==0.0.32   # required by FastAPI for file uploads
starlette==0.49.3

# ---------------------------------------------------------------------------
# ML — core
# ---------------------------------------------------------------------------
torch==2.5.1+cu121
torchvision==0.20.1+cu121
pillow==12.2.0
numpy==2.4.4

# ---------------------------------------------------------------------------
# Experiment tracking & model registry
# ---------------------------------------------------------------------------
mlflow==3.14.0
mlflow-skinny==3.14.0
sqlalchemy==2.0.51         # MLflow's database backend

# ---------------------------------------------------------------------------
# Pipeline orchestration
# ---------------------------------------------------------------------------
zenml==0.95.1

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==9.1.1
httpx==0.28.1              # required by FastAPI TestClient
